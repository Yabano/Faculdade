#include <iostream>
#include <math.h>

main(){
    float a, b, c, soma;

    printf("Digite tres valores: \n");
    scanf("%f %f %f", &a, &b, &c);

    soma = a + b + c;
    printf("a soma e: %f",soma);

}
